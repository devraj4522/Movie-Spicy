import {StyleSheet, Dimensions} from 'react-native';

export default StyleSheet.create({
    mainContainer:{
        flex:1,
        // margin: 16,
        // paddingTop:12,
        // marginTop:0,
    },
    
});